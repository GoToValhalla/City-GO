{
  "name": "ui-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "PLAYWRIGHT_HTML_OPEN=never playwright test",
    "test:ui": "PLAYWRIGHT_HTML_OPEN=never playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
