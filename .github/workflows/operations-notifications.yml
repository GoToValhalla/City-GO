name: 89 · CITY GO · MON · Operations Notifications

on:
  workflow_run:
    workflows:
      - 80 · CITY GO · MON · API Errors
      - 81 · CITY GO · MON · Admin Edge
      - 82 · CITY GO · MON · Admin API
      - 83 · CITY GO · MON · Catalog Data
      - 84 · CITY GO · MON · Production Health
      - 96 · CITY GO · DEBUG · Backend Error Diagnostics
      - 99 · CITY GO · EMERGENCY · Prod DB Repair
      - City Import
      - Data Enrichment
      - Production Docker Cleanup
      - Recover Frontend
    types:
      - completed

permissions:
  contents: read

jobs:
  notify:
    name: Notify operational workflow result
    if: github.event.workflow_run.head_repository.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout notification helper
        uses: actions/checkout@v4

      - name: Decide notification policy
        id: policy
        shell: bash
        env:
          RUN_NAME: ${{ github.event.workflow_run.name }}
          RUN_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        run: |
          set -euo pipefail
          if [ "$RUN_CONCLUSION" = "success" ]; then
            case "$RUN_NAME" in
              "80 · CITY GO · MON · API Errors"|"81 · CITY GO · MON · Admin Edge"|"82 · CITY GO · MON · Admin API"|"83 · CITY GO · MON · Catalog Data"|"84 · CITY GO · MON · Production Health"|"96 · CITY GO · DEBUG · Backend Error Diagnostics")
                echo "send=false" >> "$GITHUB_OUTPUT"
                exit 0
                ;;
            esac
          fi
          echo "send=true" >> "$GITHUB_OUTPUT"

      - name: Build operational notification
        if: steps.policy.outputs.send == 'true'
        shell: bash
        env:
          RUN_NAME: ${{ github.event.workflow_run.name }}
          RUN_NUMBER: ${{ github.event.workflow_run.run_number }}
          RUN_ATTEMPT: ${{ github.event.workflow_run.run_attempt }}
          RUN_CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          RUN_BRANCH: ${{ github.event.workflow_run.head_branch }}
          RUN_SHA: ${{ github.event.workflow_run.head_sha }}
          RUN_URL: ${{ github.event.workflow_run.html_url }}
        run: |
          set -euo pipefail
          STATUS="✅ завершён успешно"
          if [ "$RUN_CONCLUSION" != "success" ]; then
            STATUS="❌ завершился со статусом ${RUN_CONCLUSION}"
          fi
          cat > /tmp/operations-notification.txt <<EOF
          City GO · ${RUN_NAME}
          ${STATUS}
          Прогон: #${RUN_NUMBER} · попытка ${RUN_ATTEMPT}
          Ветка: ${RUN_BRANCH}
          Commit: ${RUN_SHA:0:7}
          Подробнее: ${RUN_URL}
          EOF
          sed -i 's/^          //' /tmp/operations-notification.txt

      - name: Send Telegram operational notification
        if: steps.policy.outputs.send == 'true'
        shell: bash
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python scripts/send_telegram_notification.py --text-file /tmp/operations-notification.txt
