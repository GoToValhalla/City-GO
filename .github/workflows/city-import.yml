name: City Import

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  city-import:
    name: Manual city import placeholder
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: City import placeholder
        shell: bash
        run: |
          echo "City import scaffold is ready."
          echo "Real import commands will be added after migration."
