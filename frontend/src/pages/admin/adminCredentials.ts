/**
 * TEMPORARY ADMIN ACCESS (login/password only).
 * API Bearer token: VITE_ADMIN_API_TOKEN → adminToken.ts (build variable, не в git).
 */

// ─── CHANGE THESE BEFORE DEPLOY ──────────────────────────────────────────────
export const ADMIN_LOGIN = 'admin'
export const ADMIN_PASSWORD = 'admin1234!@#$'
// ─────────────────────────────────────────────────────────────────────────────
