// Re-export from shared/place/categoryLabels to maintain backward compatibility.
export {
  categoryLabel,
  interestMatches,
  placeDescription,
  priceLabel,
  sourceLabel,
  timeLabel,
} from '../place/categoryLabels'
